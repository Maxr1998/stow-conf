#!/bin/sh
if playerctl -i "chromium" metadata xesam:title > /dev/null 2>&1; then
    playerctl -i "chromium" metadata --format "{{xesam:artist}} ・ {{xesam:title}}" | cut -c -100
else
    echo ""
fi
